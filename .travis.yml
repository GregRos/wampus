language: node_js
branches:
  only:
    - master
cache:
  yarn: true
  directories:
    - node_modules
install:
  - yarn install
script:
  - yarn run build
  - yarn run nyc node_modules/ava/cli-js --files="dist/test/tests"
  - yarn run codecov
node_js:
  - "8.9"